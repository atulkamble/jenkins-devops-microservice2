node {
	stage('Build') {
	echo "Build"
         }

	stage('Test') {
	echo "Test"
         }

	stage('Inegration Test') {
	echo "Integration Test"
         }

	stage('running')	{
		echo"Running State"
	} 
}